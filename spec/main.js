describe('${User.name} Class', () => {
	it('exists', () => {
		expect(User).toBeDefined();
	});
	let model;
	beforeEach(() => {
        model = new User();
    });
	describe('Aditional Matcher Examples', () => {
	});
});